"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.replaceBackground = replaceBackground;

var _reactNative = require("react-native");

const LINKING_ERROR = `The package 'react-native-image-selfie-segmentation' doesn't seem to be linked. Make sure: \n\n` + _reactNative.Platform.select({
  ios: "- You have run 'pod install'\n",
  default: ''
}) + '- You rebuilt the app after installing the package\n' + '- You are not using Expo managed workflow\n';
const ImageSelfieSegmentation = _reactNative.NativeModules.ImageSelfieSegmentation ? _reactNative.NativeModules.ImageSelfieSegmentation : new Proxy({}, {
  get() {
    throw new Error(LINKING_ERROR);
  }

});

function replaceBackground(inputImage, backgroundImage) {
  let maxSize = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 500;
  return ImageSelfieSegmentation.replaceBackground(inputImage, backgroundImage, maxSize);
}
//# sourceMappingURL=index.js.map